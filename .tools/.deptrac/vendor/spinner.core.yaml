# depends on '../module/extras.yaml'
deptrac:
  layers:
    - name: VendorSpinnerCore
      collectors:
        - type: bool
          must:
            - type: className
              value: '%namespace%\\.*'
          must_not:
            - type: layer
              value: Extras
